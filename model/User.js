/* schema: 필요한 데이터의 형태를 정의한다. 예). 상품: 상품명, 재고, 가격 ... */
const mongoose = require('mongoose');

const Schema = mongoose.Schema;

// User Schema
// name, email, password, register_date
const UserSchema = new Schema({

});